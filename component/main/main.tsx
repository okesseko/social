import React from 'react';
import {ScrollView, View} from 'react-native';
import Story from './story';

const Main = () => {
  return <ScrollView>
      <Story/>
  </ScrollView>;
};

export default Main;
